// IMPORTANT: Kaboom version 0.5.0 script tag in the HTML file

kaboom({
    global: true,
    fullscreen: true,
    scale: 2,
    debug: true,
    clearColor: [0, 0, 0, 1],
  })

  scene("game", () => {

  })

  start("game")
  
